import java.util.Scanner;
public class ArraysExample4 {
    public static void main(String[] args)
    {
        Scanner keyboard = new Scanner(System.in);
        System.out.print("How many array elements? ");
        int numElements = keyboard.nextInt();
        System.out.print("\n");
        double[] storeNumbers = new double[numElements];
        int maxIndex = 0;
        int minIndex = 0;
        
        for(int arrayIndex = 0; arrayIndex < numElements; arrayIndex++)
        {
            System.out.print("Enter floating-point number " + (arrayIndex + 1) + ": ");
            
        }
        
        
        
        
        
    }
}
